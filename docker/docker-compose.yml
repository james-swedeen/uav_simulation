version: "2"

services:
  uav_sim:
    image: uav_sim
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    network_mode: host
    #command: bash -c ""


